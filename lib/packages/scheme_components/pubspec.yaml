name: scheme_components
description: A Package Containing Scheme Widgets and Styles
version: 0.0.5
author:
homepage:

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_shine:
  get:
  flutter_svg:
  sa_stateless_animation:
  flutter_hooks:
  streaming_shared_preferences:
  get_it:
  google_fonts:
  flare_flutter:
  url_launcher:
  lottie:
  palette_generator:
  animations:
  rxdart:
  injectable_generator:
  build_runner:
  scheme_shared:
    git:
      url: "https://github.com/Ammonious/Scheme-Development.git"
      path: "lib/packages/scheme_shared/"
  scheme_theme:
    git:
      url: "https://github.com/Ammonious/Scheme-Development.git"
      path: "lib/packages/scheme_theme/"
  scheme_icons:
    git:
      url: "https://github.com/Ammonious/Scheme-Development.git"
      path: "lib/packages/scheme_icons/"
  scheme_utilities:
    git:
      url: "https://github.com/Ammonious/Scheme-Development.git"
      path: "lib/packages/scheme_utilities/"

dev_dependencies:
  build_runner:


flutter:
  assets:
    - assets/illustrations/flare/
    - assets/illustrations/lottie/
    - assets/illustrations/shape/
    - assets/other/

