import 'package:flutter/material.dart';

class SchemeNavBar extends StatelessWidget {
SchemeNavBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(color:Colors.transparent,);
  }
}
