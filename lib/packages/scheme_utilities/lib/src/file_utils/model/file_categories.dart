import 'package:flutter/material.dart';

class FileCategory {
	final String title;
	final String path;
	final Widget icon;
	final Color color;

  FileCategory({this.title, this.path, this.icon, this.color});
}